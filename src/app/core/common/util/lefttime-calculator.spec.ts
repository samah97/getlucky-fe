import { LefttimeCalculator } from './lefttime-calculator';

describe('LefttimeCalculator', () => {
  it('should create an instance', () => {
    expect(new LefttimeCalculator()).toBeTruthy();
  });
});
