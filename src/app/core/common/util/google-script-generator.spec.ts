import { GoogleScriptGenerator } from './google-script-generator';

describe('GoogleScriptGenerator', () => {
  it('should create an instance', () => {
    expect(new GoogleScriptGenerator()).toBeTruthy();
  });
});
